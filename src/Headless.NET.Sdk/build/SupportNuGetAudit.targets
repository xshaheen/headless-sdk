<Project>
  <!-- 👇 Provides warnings during restore if any packages used by your project have known vulnerabilities -->

  <PropertyGroup Condition="'$(NuGetAudit)' == ''">
    <NuGetAudit>true</NuGetAudit>
  </PropertyGroup>

  <PropertyGroup Condition="'$(NuGetAuditMode)' == ''">
    <NuGetAuditMode>all</NuGetAuditMode>
  </PropertyGroup>

  <PropertyGroup Condition="'$(NuGetAuditLevel)' == ''">
    <NuGetAuditLevel>low</NuGetAuditLevel>
  </PropertyGroup>

  <PropertyGroup Condition="$(ContinuousIntegrationBuild) == 'true' OR '$(Configuration)' == 'Release'">
    <!--
      NU1901 - Found a package with a vulnerability with level 'unknown' in the project.
      NU1902 - Found a package with a vulnerability with level 'low' in the project.
      NU1903 - Found a package with a vulnerability with level 'medium' in the project.
      NU1904 - Found a package with a vulnerabilitywith level 'high' in the project.
      see: https://learn.microsoft.com/en-us/nuget/reference/errors-and-warnings/nu1901-nu1904

      NU1900 is intentionally NOT escalated: it means the audit source could not be reached (offline,
      registry outage, transient network error), not that a vulnerability was found. Escalating it
      would turn a connectivity blip into a hard CI/Release build failure. It stays a warning.
    -->
    <WarningsAsErrors>$(WarningsAsErrors);NU1901;NU1902;NU1903;NU1904</WarningsAsErrors>
  </PropertyGroup>
</Project>
