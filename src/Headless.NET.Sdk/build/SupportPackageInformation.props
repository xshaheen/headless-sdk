<Project>
  <!--
    PackageTags depends on project-body IsTestProject / IsTestHarnessProject values, so the
    default is applied in SupportPackageInformation.targets after the consumer project evaluates.
  -->
</Project>
