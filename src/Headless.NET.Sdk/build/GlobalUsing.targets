<Project>
  <!--
    Evaluate after the consumer project body so an explicit ImplicitUsings value is authoritative
    in every consumption mode. .NET 10 is the sole supported consumer target, so every namespace
    below is available whenever the Microsoft implicit-usings feature is enabled.
  -->
  <ItemGroup
    Condition=" ('$(ImplicitUsings)' == 'enable' or '$(ImplicitUsings)' == 'true')
      and '$(TargetFramework)' != ''
      and $(TargetFramework.StartsWith('net10.0')) "
  >
    <Using Include="System.Diagnostics.CodeAnalysis" />
    <Using Include="System.Globalization" />
    <Using Include="System.Text" />
    <Using Include="System.Text.Json" />
    <Using Include="System.Text.Json.Serialization" />
    <Using Include="System.Collections.Frozen" />
    <Using Include="System.Collections.Immutable" />
  </ItemGroup>
</Project>
