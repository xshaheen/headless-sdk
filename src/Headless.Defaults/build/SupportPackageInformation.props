<Project>
  <PropertyGroup Label="NuGet Package Headless.Defaults" Condition=" '$(IsTestableProject)' != 'true' ">
    <PackageTags>xshaheen;$(PackageTags)</PackageTags>
  </PropertyGroup>
</Project>
